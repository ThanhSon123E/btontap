#include "stdio.h"
#include "conio.h"
#define MAX 100

int InMang(int a[], int n); //In danh sach mang
int TinhTich(int a[], int n); // Tinh tich cac phan tu trong mang
int Max(int a[], int n); // In ra gia tri Lon nhat trong mang
int DemSoChan(int a[], int n); //Dem so chan
int SapXepTang(int a[], int n); // In mang theo thu tu tang dan

int main()
{
	int a[MAX]={1,0,5,7,9,6};
	int n = 5;
	//InMang(a,n);
	//TinhTich(a,n);
	//Max(a,n);
	//DemSoChan(a,n);
	//SapXepTang(a,n);
	getch();
	return 0;
}
